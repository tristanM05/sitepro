{% extends 'base_admin.html.twig' %}

{% block body %}
<div class="container">
<a href="{{path('admin')}}" class="btn btn-info mt-5 mb-3"><i class="fas fa-arrow-left mr-2"></i>Retour</a>
    <div class="card cardCateg">
        <div class="card-header p-0">
            <p class="text-center titleCard">Ajouter une catégorie</p>
        </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-lg-6 col-sm-12 mt-5">
                        {{form_start(form)}}
                        {{form_row(form.name)}}
                        {{form_row(form.description)}}
                        {{form_row(form.client)}}
                        {{form_row(form.images)}}
                        <input type="submit" class="btn btn-info btn-block btn1 p-0" value="Valider">
                        {{form_end(form)}}
                    </div>
                    <div class="col-6">
                        <img src="/images/un2.png" width="500" height="350" alt="" id="imgAdmin1">
                    </div>
                </div>
            </div>
    </div>
</div>
{% endblock %}